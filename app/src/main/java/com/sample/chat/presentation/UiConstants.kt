package com.sample.chat.presentation

import androidx.compose.foundation.shape.CornerSize
import androidx.compose.ui.unit.dp

val messageRoundCorner = CornerSize(10.dp)
val zeroRoundCorner = CornerSize(0.dp)
